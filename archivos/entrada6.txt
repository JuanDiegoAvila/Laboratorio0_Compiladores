class Main {
    id_1 : Int <- 0;
    main() : Object {
        let x : Int <- 5 in
        {
            while x = 0 loop
                out_string("Countdown: ")
            pool ;
        }
    };

    count( initial : Int ) : Int {
        initial <- ~ ( 1 + 1 )
    };
};