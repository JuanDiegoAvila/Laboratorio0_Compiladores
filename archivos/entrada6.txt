class Main {
    id_1 : Int <- 0;
    print: IO;
    str : String <- "Hello World";
    main() : Object {
        let x : Int <- 5 in
        {
            while id_1 loop
                print.out_string("Countdown: ")
            pool ;
        }
    };

    count( initial : Int ) : Int {
        initial <- ~ ( false + 1 )
    };
};